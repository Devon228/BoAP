#pragma once

#include <iostream>
#include <string>

typedef long long ll;

void init(short);
void cinLong(ll&);
void cinN(int* num, int len);
void coutN(int* num, int len, int base);
void minus1 (int* num, int len, int base);
void inv (int* num, int len, int base);