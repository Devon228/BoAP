#pragma once

#include <iostream>
#include <string>

typedef long long ll;

void init(short);
void cinLong(ll&);
std::string toBase3 (int);
void procAns(bool*, int);
void null_arr (int*, int, int);
void makeDay1 (int*, int*);
void makeDay2 (int*, int*, bool*);